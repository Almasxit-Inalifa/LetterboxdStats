name: letterboxd
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5  
  url_launcher: ^6.1.9
  http: ^1.2.2
  html: ^0.15.0
  flutter_svg: ^2.0.10+1
  cupertino_icons: ^1.0.6
  fl_chart: ^0.68.0
  country_flags: ^3.0.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/logo.png

